PORT=3001
FRONTEND_URL=http://localhost:5173
# PostgreSQL connection string (defaults to internal storage engine if not running)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pulsechat
# Redis Pub/Sub URL (defaults to high-speed in-memory bus if not running)
REDIS_URL=redis://localhost:6379
NODE_ENV=development
